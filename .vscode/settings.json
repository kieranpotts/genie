{

  /* Vertical guidelines for line wrapping. */
  "editor.rulers": [
    80,
    100,
    120,
    140,
    160
  ],

  /* By default, wrap long lines at the viewport or 120 characters,
  whichever is narrowest. */
  "editor.wordWrap": "bounded",
  "editor.wordWrapColumn": 120,

  /* Errno and sigstore identifiers that appear in the infrastructure docs and
  the runbook's egress check. These are literal values an operator matches
  against command output, so they cannot be reworded. */
  "cSpell.words": [
    "EAI_AGAIN",
    "ECONNREFUSED",
    "ENETUNREACH",
    "Errno",
    "EROFS",
    "litellm",
    "Rekor",
    "sigstore",
    "tmpfs",
  ],

  "[markdown]": {
    /* Wrap documentation files at the viewport width. Hard-wrapping
    should be applied at 80 characters, except for tables and long URLs. */
    "editor.wordWrap": "on",

    /* Enable auto-formatting for Markdown files. */
    "editor.formatOnPaste": true,
    "editor.formatOnSave": true,
    "editor.formatOnType": true,

    /* Enable auto-fixes for Markdown files. */
    "editor.codeActionsOnSave": {
      "source.fixAll": "always",
    },
  },

}
